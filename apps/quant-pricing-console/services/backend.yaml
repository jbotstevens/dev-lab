---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: quant-pricing-console-backend
spec:
  interval: 1h0m0s
  chart:
    spec:
      chart: ./charts/backend
      sourceRef:
        kind: GitRepository
        name: quant-traders-dash
        namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: qs-common-config
      valuesKey: values.yaml
    - kind: ConfigMap
      name: qpc-common-config
      valuesKey: values.yaml
  values:
    image:
      name: backend
      tag: 1.0.0
