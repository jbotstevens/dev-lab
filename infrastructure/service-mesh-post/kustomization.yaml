apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: dev-lab-service-mesh-post
  namespace: linkerd

resources:
  - linkerd-certs.yaml

configurations:
  - kustomizeconfig.yaml